spring:
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      # k6 시드 데이터만 로드 (절대경로는 워크플로에서 K6_SEED_PATH로 주입)
      data-locations: file:${K6_SEED_PATH}
  datasource:
    url: jdbc:h2:mem:gymbarofit;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  h2:
    console:
      enabled: false

app:
  upload:
    dir: /tmp/gymbarofit-ci
  jwt:
    refresh-token:
      secure-cookie: false

logging:
  level:
    org.hibernate.SQL: WARN
    org.springframework.security: WARN
